%h1 
	All Labels

- if @labels
	%ul
		- @labels.each do |at|
			%li
				= at.group.name 
				-
				= link_to(at.name, admin_label_path(at))	
- else
	No Records...

%p
	= link_to('Add New', new_admin_label_path)